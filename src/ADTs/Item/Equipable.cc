#include "Equipable.h"
#include <string>
using namespace std;

// map<string,StatMod> Equipable::getPassive(){
    
// }

Equipable::Equipable(string _name){
    setString(_name);
}
Equipable::~Equipable(){
    
}